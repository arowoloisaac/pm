import React from "react";
import Axios from "axios";
import { ApiUrl, Token } from "@/components/Storage/Storage";

const editProject = ({ data }: { data: {} }) => {
  const response = Axios.post("");
  return null;
};

export { editProject };
